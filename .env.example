# Gemini API Key (Optional - users can also provide their own in the UI)
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=GEMINI_KEY

# Optional: Set to 'true' to require server-side API key only
REQUIRE_SERVER_API_KEY=false
